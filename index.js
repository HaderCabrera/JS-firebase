window.addEventListener('DOMContentLoaded', () => {

})

const taskForm = document.getElementById('task-form')
taskForm.addEventListener('onclick', (e) => {
    e.preventDefault;
    console.log("estoy enviando algo")

})